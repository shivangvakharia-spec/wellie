{% doc %}
  Search Icon

  @param {string} [class] - Additional CSS classes (optional)
  @param {number} size - Icon size in pixels (default: 20)
{% enddoc %}

{%- assign icon_size = size | default: 20 -%}
<svg
  class="{{ class }}"
  width="{{ icon_size }}"
  height="{{ icon_size }}"
  viewBox="0 0 24 24"
  fill="none"
  aria-hidden="true"
>
  <circle cx="11.5" cy="11.5" r="8.5" stroke="currentColor" stroke-width="1.5"/>
  <path d="M21 21L18 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="square"/>
</svg>
