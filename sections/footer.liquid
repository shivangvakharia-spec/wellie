{%- liquid
  assign current_year = 'now' | date: '%Y'
-%}

<footer
  class="footer"
  style="
    background-color: {{ section.settings.background_color }};
    color: {{ section.settings.text_color }};
    --footer-padding-top: {{ section.settings.padding_top }}px;
    --footer-padding-bottom: {{ section.settings.padding_bottom }}px;
  "
>
  <div class="page-width" style="padding-top: var(--footer-padding-top); padding-bottom: var(--footer-padding-bottom);">
    <div class="grid gap-12 md:grid-cols-12 lg:gap-16">
      {%- comment -%} Logo & description {%- endcomment -%}
      <div class="md:col-span-4">
        {%- if settings.logo != blank -%}
          <a href="{{ routes.root_url }}" class="mb-4 inline-block">
            <img
              src="{{ settings.logo | image_url: width: 200 }}"
              alt="{{ shop.name }}"
              width="{{ settings.logo_width | default: 120 }}"
              height="auto"
              loading="lazy"
            >
          </a>
        {%- else -%}
          <a href="{{ routes.root_url }}" class="mb-4 inline-block font-primary text-2xl font-normal tracking-tight">
            {{ shop.name }}
          </a>
        {%- endif -%}

        {%- if section.settings.description != blank -%}
          <div class="mt-4 text-sm leading-loose opacity-70">
            {{ section.settings.description }}
          </div>
        {%- endif -%}

        {%- comment -%} Social links {%- endcomment -%}
        {%- assign social_links = 'facebook,twitter,instagram,youtube,tiktok,pinterest,linkedin' | split: ',' -%}
        {%- assign has_social = false -%}
        {%- for platform in social_links -%}
          {%- assign setting_id = 'social_' | append: platform -%}
          {%- if settings[setting_id] != blank -%}
            {%- assign has_social = true -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}

        {%- if has_social -%}
          <div class="mt-6 flex gap-5">
            {%- for platform in social_links -%}
              {%- assign setting_id = 'social_' | append: platform -%}
              {%- if settings[setting_id] != blank -%}
                <a
                  href="{{ settings[setting_id] }}"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-sm opacity-60 transition-all duration-200 hover:opacity-100"
                  aria-label="{{ platform | capitalize }}"
                >
                  {{ platform | capitalize }}
                </a>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>

      {%- comment -%} Menu columns {%- endcomment -%}
      <div class="md:col-span-8">
        {%- if section.settings.menu != blank -%}
          <ul class="grid grid-cols-2 gap-8">
            {%- for link in linklists[section.settings.menu].links -%}
              <li>
                <a
                  href="{{ link.url }}"
                  class="block text-[11px] font-medium uppercase tracking-[0.25em] hover:underline hover:underline-offset-4 break-words"
                >
                  {{ link.title }}
                </a>
                {%- if link.links.size > 0 -%}
                  <ul class="space-y-3">
                    {%- for child_link in link.links -%}
                      <li>
                        <a
                          href="{{ child_link.url }}"
                          class="text-sm opacity-60 transition-all duration-200 hover:opacity-100"
                        >
                          {{ child_link.title }}
                        </a>
                      </li>
                    {%- endfor -%}
                  </ul>
                {%- endif -%}
              </li>
            {%- endfor -%}
          </ul>
        {%- endif -%}
      </div>
    </div>

    {%- comment -%} Copyright {%- endcomment -%}
    <div class="mt-16 border-t border-white/10 pt-8 text-sm opacity-60">
      {%- if section.settings.copyright_text != blank -%}
        {{ section.settings.copyright_text | replace: 'YEAR', current_year }}
      {%- else -%}
        &copy; {{ current_year }}
        {{ shop.name }}. All rights reserved.
      {%- endif -%}
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "class": "footer-section",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "footer"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description"
    },
    {
      "type": "richtext",
      "id": "copyright_text",
      "label": "Copyright text",
      "info": "Use YEAR to show the current year"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#0a0a0a"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 100,
      "default": 80,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 100,
      "default": 60,
      "unit": "px"
    }
  ]
}
{% endschema %}
