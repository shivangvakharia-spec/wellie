<header
  class="header sticky top-0 z-50 w-full transition-all duration-300"
  id="main-header"
  style="background-color: {{ section.settings.background_color }}; color: {{ section.settings.text_color }};"
>
  {%- comment -%} Desktop header {%- endcomment -%}
  {% render 'header-desktop', section: section %}

  {%- comment -%} Mobile header {%- endcomment -%}
  {% render 'header-mobile', section: section %}
</header>

{% render 'search-modal' %}

<script src="{{ 'accordion.js' | asset_url }}" defer></script>
<script src="{{ 'header-javascript.js' | asset_url }}" defer></script>
<script src="{{ 'search-recent-searches.js' | asset_url }}" defer></script>
<script src="{{ 'search-recently-viewed.js' | asset_url }}" defer></script>
<script src="{{ 'search-modal.js' | asset_url }}" defer></script>
<script src="{{ 'predictive-search.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#F9F3F1"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text and icon color",
      "default": "#025A60"
    }
  ],
  "blocks": [
    {
      "type": "mega_menu",
      "name": "Mega menu",
      "settings": [
        {
          "type": "text",
          "id": "menu_item",
          "label": "Menu item",
          "info": "Enter the exact title of the menu item to apply a mega menu."
        },
        {
          "type": "header",
          "content": "Image 1"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "image_heading_1",
          "label": "Heading"
        },
        {
          "type": "url",
          "id": "image_link_1",
          "label": "Link"
        },
        {
          "type": "header",
          "content": "Image 2"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "image_heading_2",
          "label": "Heading"
        },
        {
          "type": "url",
          "id": "image_link_2",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Header"
    }
  ]
}
{% endschema %}
